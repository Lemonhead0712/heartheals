{
  "framework": "nextjs",
  "buildCommand": "rm -rf node_modules .next && pnpm install --force && NEXT_DISABLE_SOURCEMAPS=false NEXT_DISABLE_MINIFICATION=true pnpm build",
  "installCommand": "pnpm install --force",
  "cleanUrls": true,
  "env": {
    "NEXT_DISABLE_MINIFICATION": "true",
    "NEXT_DISABLE_SOURCEMAPS": "false"
  }
}
