# Use strict resolution to avoid dependency conflicts
resolution-mode=highest
# Ensure pnpm uses the correct Node.js version
engine-strict=true
# Avoid using the deprecated Webpack 4
prefer-workspace-packages=true
